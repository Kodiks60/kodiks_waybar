# kodiks waybar
waybar config for you :)

How install him
1. Copy the repository:

```bash
git clone https://github.com/Kodiks60/kodiks_waybar
```
2. Let's setup
   If you have arch linux/manjaro or you have pacman manager use arch.sh
```bash
cd kodiks_waybar
chmod +x arch.sh
./arch.sh
```
